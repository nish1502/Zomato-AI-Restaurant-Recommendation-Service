"""
Phase 2: filtering engine, heuristic ranking, and recommendation schemas.

Canonical implementations for Phase 2 live in this package. Thin wrappers
under `app.services` and `app.schemas` re-export the public functions and
models for backwards compatibility.
"""

